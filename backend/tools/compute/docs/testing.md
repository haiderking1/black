# Tests

Run bun test backend/tools/compute for the copied worker, provider, schema, recovery, and result tests. Run bun test for the full application suite. Tests use local files and subprocesses, not paid model requests.

Run bunx tsc --noEmit and bun run build to check integration and worker packaging.
